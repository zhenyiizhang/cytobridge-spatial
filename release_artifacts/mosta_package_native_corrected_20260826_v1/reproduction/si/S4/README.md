# MOSTA Supplementary Figure S4 corrected reproduction

Panel-specific working bundle. Numerical truth comes from the immutable latest-package 50k dense global-t0 shared run. Style truth comes from submitted SI page 30 and the historical `mosta_interpolated_slices.ipynb` renderer/palette.

The bundle remains mutable until numerical audit, vector PDF/SVG rendering, Poppler QA, and provenance sealing pass.
