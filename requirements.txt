# Compatibility install for development environments that historically used
# ``pip install -r requirements.txt``.  Wheel users should prefer a named
# extra such as ``CytoBridge[spatial]`` or ``CytoBridge[all]``.
-r requirements/core.txt
-r requirements/all.txt
